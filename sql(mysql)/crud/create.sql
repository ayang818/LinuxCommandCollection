-- create database
create database database_name;

-- create table
create table user (id bigint(20) auto_increment primary key not null, username varchar(50), password varchar(50));

-- insert value
insert into table_name values(**, **);